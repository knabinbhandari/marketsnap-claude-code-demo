#!/usr/bin/env bash
#
# SessionStart hook.
# Loads team-specific context dynamically at the beginning of every Claude
# Code session. Real implementation would, for example, fetch the developer's
# Confluence page based on the directory they're working in.
#
# This placeholder is a no-op so the screenshot in the video shows a real
# hooks setup wired into settings.json. Replace with real logic when adopting.

exit 0
